# Project Agent Defaults

This file is generated by `local-agent` to keep Claude and Codex aligned in new projects.

Update the shared baseline in `local-agent/templates/home/AGENTS.md`, not in this generated copy.

## Shared Baseline

<!-- BEGIN local-agent shared baseline -->
# Shared Agent Defaults

This file is the shared cross-agent source of truth for machine-level preferences.

If a preference should apply to both Claude and Codex, record it here rather than relying on agent-specific memory.

## Communication Defaults

Operate in caveman mode (full intensity) by default for conversational replies. See `~/.claude/skills/caveman/SKILL.md`.

Write in normal prose, not caveman, whenever output is a deliverable that will be read outside the chat:
- Code, commits, PR descriptions, commit messages
- UX copy suggestions, microcopy, error messages, user-facing strings
- Plans, design briefs, PRDs, architecture docs, shape output, lean-scope output
- Documentation, README and PRD edits, JSDoc, inline doc comments
- Audit, critique, and review reports
- Security warnings and irreversible-action confirmations

Caveman applies to status updates, questions, explanations of what just changed, and casual back-and-forth. Resume caveman after any deliverable block.

Turn off fully on "stop caveman" or "normal mode".
<!-- END local-agent shared baseline -->

## Project Context

- Product/domain context:
  This repo is the tracked source of truth for personal Claude + Codex setup across machines and projects. It owns templates, portable skills, manifests, bootstrap/doctor tooling, and project-init scaffolding.
- Build, test, and verification commands:
  Run `python3 -m unittest discover -s tests -v` before closing script changes.
  Use `python3 scripts/local_agent.py doctor` to check home drift.
  Use `python3 scripts/local_agent.py doctor --project /path/to/project --expect-impeccable` to check a project bootstrap.
  Use `python3 scripts/local_agent.py sync --apply` to apply tracked home changes.
- Project-specific constraints and preferences:
  Keep secrets out of git; only `config/machine.example.json` is tracked, while `config/machine.local.json` stays ignored.
  Prefer changing tracked templates/manifests/portable skills instead of hand-editing live files under `~/.claude`, `~/.codex`, or `~/.agents`.
  Keep bootstrap behavior idempotent and conservative; destructive adoption should stay opt-in.
