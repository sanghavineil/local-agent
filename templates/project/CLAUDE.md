@AGENTS.md

# Claude-only project note

Put project-specific Claude-only notes here only if they cannot live in `AGENTS.md`.
Shared rules, workflow, and project context should stay in `AGENTS.md` so every agent can consume the same source of truth.
